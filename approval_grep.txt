src/components/PresidentDashboard.tsx:  const approvalColor = pl.approvalRating > 60 ? 'text-emerald-400' : pl.approvalRating > 40 ? 'text-yellow-400' : 'text-red-400';
src/components/PresidentDashboard.tsx:            <div className={`text-4xl font-serif font-black ${approvalColor}`}>{pl.approvalRating}%</div>
src/store/initialState.ts:    approvalRating: 50,
src/store/slices/presidentSlice.ts:      approvalRating: Math.max(0, Math.min(100, state.pl.approvalRating + approvalImpact)),
src/store/slices/presidentSlice.ts:        approval: pl.approvalRating
src/store/slices/presidentSlice.ts:      approvalRating: Math.max(0, Math.min(100, updatedPl.approvalRating + approvalHit + cabinetApproval)),
src/store/slices/presidentSlice.ts:      if (updatedPl.approvalRating > 60) newSupport = 70;
src/store/slices/presidentSlice.ts:      else if (updatedPl.approvalRating < 40) newSupport = 30;
src/store/slices/presidentSlice.ts:      if (updatedPl.approvalRating > 50) {
src/store/slices/presidentSlice.ts:        updatedPl.approvalRating = Math.max(0, Math.min(100, updatedPl.approvalRating - 10));
src/engine/statEngine.ts:    approvalRating: Math.max(0, Math.min(100, pl.approvalRating)),
src/tests/presidency_new_mechanics.test.ts:        approvalRating: 50,
src/tests/presidency_new_mechanics.test.ts:    store.setState((state) => ({ pl: { ...state.pl, presidentMonth: 23, approvalRating: 65 } }));
src/tests/presidency_new_mechanics.test.ts:    store.setState((state) => ({ pl: { ...state.pl, inflation: 6, approvalRating: 50 } }));
src/tests/presidency_new_mechanics.test.ts:    expect(getState().pl.approvalRating).toBeLessThan(50); // Should have hit -2 approval decay
src/tests/presidency_new_mechanics.test.ts:    const approvalBeforeTax = getState().pl.approvalRating;
src/tests/presidency_new_mechanics.test.ts:    expect(getState().pl.approvalRating - approvalBeforeTax).toBe(5);
src/tests/presidency_clamping.test.ts:  it('should clamp approvalRating between 0 and 100', () => {
src/tests/presidency_clamping.test.ts:    store.setState(s => ({ pl: { ...s.pl, approvalRating: 95, clout: 1000, aura: 1000, federalBudget: 100000000 } }));
src/tests/presidency_clamping.test.ts:    expect(store.getState().pl.approvalRating).toBe(100);
src/tests/presidency_clamping.test.ts:    store.setState(s => ({ pl: { ...s.pl, approvalRating: 5 } }));
src/tests/presidency_clamping.test.ts:    expect(store.getState().pl.approvalRating).toBe(0);
src/tests/presidency_clamping.test.ts:            approvalRating: 150, // Should be clamped by enforceStatCaps next set
src/tests/presidency_clamping.test.ts:    expect(store.getState().pl.approvalRating).toBeLessThanOrEqual(100);
src/tests/presidency_clamping.test.ts:    expect(store.getState().pl.approvalRating).toBe(100);
src/types/game.ts:  approvalRating: number;
src/components/PresidentDashboard.tsx:  const approvalColor = pl.approvalRating > 60 ? 'text-emerald-400' : pl.approvalRating > 40 ? 'text-yellow-400' : 'text-red-400';
src/components/PresidentDashboard.tsx:            <div className={`text-4xl font-serif font-black ${approvalColor}`}>{pl.approvalRating}%</div>
src/components/PresidentDashboard.tsx:            <div className="text-[10px] text-slate-400 font-bold uppercase">National Approval</div>
src/components/PresidentDashboard.tsx:              ⚠️ CRITICAL INFLATION: Approval decaying -2%/mo
src/components/PresidentDashboard.tsx:                      <div className="text-[10px] font-black text-emerald-400 bg-emerald-400/10 px-2 py-0.5 rounded">+{order.impact.approval}% PROJ. APPR</div>
src/components/PresidentDashboard.tsx:              <h2 className="text-xs font-black text-slate-500 uppercase tracking-widest">REGIONAL APPROVAL</h2>
src/components/PresidentDashboard.tsx:                {Object.entries(pl.regionalApproval || {}).map(([region, score]) => (
src/components/PresidentDashboard.tsx:              <h2 className="text-xs font-black text-slate-500 uppercase tracking-widest">DEMOGRAPHIC APPROVAL</h2>
src/components/PresidentDashboard.tsx:                {Object.entries(pl.demographicApproval).map(([group, score]) => (
src/components/PresidentDashboard.tsx:                          <span className={`text-lg font-serif font-black ${report.approval > 50 ? 'text-emerald-400' : 'text-red-400'}`}>{report.approval}% Approval</span>
src/store/initialState.ts:    approvalRating: 50,
src/store/initialState.ts:    demographicApproval: {
src/store/initialState.ts:    regionalApproval: {
src/store/slices/presidentSlice.ts:    let approvalImpact = order.impact.approval;
src/store/slices/presidentSlice.ts:      approvalImpact = Math.floor(approvalImpact * 0.5);
src/store/slices/presidentSlice.ts:      if (member.bonus.type === 'approval') {
src/store/slices/presidentSlice.ts:        approvalImpact = Math.ceil(approvalImpact * (1 + effectiveBonusValue / 100));
src/store/slices/presidentSlice.ts:      outcome: `Successfully issued the ${order.name}. Approval adjusted by ${approvalImpact > 0 ? '+' : ''}${approvalImpact}%.`,
src/store/slices/presidentSlice.ts:    const newDemographics = { ...state.pl.demographicApproval };
src/store/slices/presidentSlice.ts:    const newRegionalApproval = { ...state.pl.regionalApproval };
src/store/slices/presidentSlice.ts:        newRegionalApproval[region] = Math.min(100, Math.max(0, (newRegionalApproval[region] || 50) + impact));
src/store/slices/presidentSlice.ts:      approvalRating: Math.max(0, Math.min(100, state.pl.approvalRating + approvalImpact)),
src/store/slices/presidentSlice.ts:      demographicApproval: newDemographics,
src/store/slices/presidentSlice.ts:      regionalApproval: newRegionalApproval,
src/store/slices/presidentSlice.ts:    let approvalHit = 0;
src/store/slices/presidentSlice.ts:      approvalHit += (c.impact.approval * 1.5); // 50% extra penalty for expiration
src/store/slices/presidentSlice.ts:        outcome: `The administration failed to resolve ${c.name} in time. Massive approval hit.`,
src/store/slices/presidentSlice.ts:      approvalHit += c.impact.approval;
src/store/slices/presidentSlice.ts:      if (i.impact.approval) approvalHit += i.impact.approval;
src/store/slices/presidentSlice.ts:      const appImpact = i.impact.approval || 0;
src/store/slices/presidentSlice.ts:      state.addTickerMessage(`LEGACY IMPACT: ${i.message} (${appImpact > 0 ? '+' : ''}${appImpact}% Approval)`, 'text-slate-300 font-bold');
src/store/slices/presidentSlice.ts:      approvalHit -= 2;
src/store/slices/presidentSlice.ts:      state.addTickerMessage("PUBLIC UNREST: High inflation is hurting your approval!", "text-red-400 animate-pulse");
src/store/slices/presidentSlice.ts:        approvalHit -= appPenalty;
src/store/slices/presidentSlice.ts:        state.addTickerMessage(`SCANDAL: ${member.name} (${member.role}) leaked damaging info! Approval -${appPenalty}%`, 'text-red-600 font-black');
src/store/slices/presidentSlice.ts:          outcome: `${member.name} leaked damaging info to the press. Approval plummeted.`,
src/store/slices/presidentSlice.ts:        approval: pl.approvalRating
src/store/slices/presidentSlice.ts:    const newDemographics = { ...pl.demographicApproval };
src/store/slices/presidentSlice.ts:    let cabinetApproval = 0;
src/store/slices/presidentSlice.ts:        case 'approval': cabinetApproval += effectiveBonus; break;
src/store/slices/presidentSlice.ts:      approvalRating: Math.max(0, Math.min(100, updatedPl.approvalRating + approvalHit + cabinetApproval)),
src/store/slices/presidentSlice.ts:      demographicApproval: newDemographics,
src/store/slices/presidentSlice.ts:      if (updatedPl.approvalRating > 60) newSupport = 70;
src/store/slices/presidentSlice.ts:      else if (updatedPl.approvalRating < 40) newSupport = 30;
src/store/slices/presidentSlice.ts:      state.addTickerMessage(`MIDTERMS: Congress support adjusted to ${newSupport}% based on approval.`, 'text-blue-300 font-bold');
src/store/slices/presidentSlice.ts:      if (updatedPl.approvalRating > 50) {
src/store/slices/presidentSlice.ts:        updatedPl.approvalRating = Math.max(0, Math.min(100, updatedPl.approvalRating - 10));
src/store/slices/presidentSlice.ts:        gameOverCause = "Election Lost: The people have spoken. Your approval was too low for a second term.";
src/engine/presidentEngine.ts:  { id: 'press', role: 'Press Secretary', bonusType: 'approval' as const },
src/engine/presidentEngine.ts:    description: 'Boost approval and stimulate the economy.',
src/engine/presidentEngine.ts:      approval: 10,
src/engine/presidentEngine.ts:      approval: 15,
src/engine/presidentEngine.ts:      approval: 5,
src/engine/presidentEngine.ts:        impact: { approval: -5, heat: 15 },
src/engine/presidentEngine.ts:      approval: 5,
src/engine/presidentEngine.ts:        impact: { approval: -10, inflation: 2 },
src/engine/presidentEngine.ts:      approval: -10,
src/engine/presidentEngine.ts:        impact: { approval: 15, gdp: 2 },
src/engine/presidentEngine.ts:      approval: 10,
src/engine/presidentEngine.ts:    description: 'Significant boost to approval but extremely costly.',
src/engine/presidentEngine.ts:      approval: 30,
src/engine/presidentEngine.ts:      approval: -15,
src/engine/presidentEngine.ts:      approval: -20,
src/engine/presidentEngine.ts:      approval: -10,
src/engine/statEngine.ts:    approvalRating: Math.max(0, Math.min(100, pl.approvalRating)),
src/tests/presidency_new_mechanics.test.ts:        approvalRating: 50,
src/tests/presidency_new_mechanics.test.ts:    store.setState((state) => ({ pl: { ...state.pl, presidentMonth: 23, approvalRating: 65 } }));
src/tests/presidency_new_mechanics.test.ts:    expect(getState().pl.congressSupport).toBe(70); // High approval midterm
src/tests/presidency_new_mechanics.test.ts:    store.setState((state) => ({ pl: { ...state.pl, inflation: 6, approvalRating: 50 } }));
src/tests/presidency_new_mechanics.test.ts:    expect(getState().pl.approvalRating).toBeLessThan(50); // Should have hit -2 approval decay
src/tests/presidency_new_mechanics.test.ts:    const approvalBeforeTax = getState().pl.approvalRating;
src/tests/presidency_new_mechanics.test.ts:    // Tax cut base approval is 10. With 50% penalty it should be 5.
src/tests/presidency_new_mechanics.test.ts:    expect(getState().pl.approvalRating - approvalBeforeTax).toBe(5);
src/tests/presidency_clamping.test.ts:  it('should clamp approvalRating between 0 and 100', () => {
src/tests/presidency_clamping.test.ts:    store.setState(s => ({ pl: { ...s.pl, approvalRating: 95, clout: 1000, aura: 1000, federalBudget: 100000000 } }));
src/tests/presidency_clamping.test.ts:    // Issue an order that gives +10 approval
src/tests/presidency_clamping.test.ts:    expect(store.getState().pl.approvalRating).toBe(100);
src/tests/presidency_clamping.test.ts:    store.setState(s => ({ pl: { ...s.pl, approvalRating: 5 } }));
src/tests/presidency_clamping.test.ts:    // Trade Tariffs gives -10 approval
src/tests/presidency_clamping.test.ts:    expect(store.getState().pl.approvalRating).toBe(0);
src/tests/presidency_clamping.test.ts:    // Force high approval and advance to re-election
src/tests/presidency_clamping.test.ts:            approvalRating: 150, // Should be clamped by enforceStatCaps next set
src/tests/presidency_clamping.test.ts:    expect(store.getState().pl.approvalRating).toBeLessThanOrEqual(100);
src/tests/presidency_clamping.test.ts:        store.getState().issueExecutiveOrder('healthcare'); // +30 approval
src/tests/presidency_clamping.test.ts:    expect(store.getState().pl.approvalRating).toBe(100);
src/types/game.ts:    type: 'approval' | 'cash' | 'clout' | 'aura';
src/types/game.ts:    approval: number;
src/types/game.ts:    approval: number;
src/types/game.ts:  approvalRating: number;
src/types/game.ts:  demographicApproval: Record<string, number>;
src/types/game.ts:  regionalApproval: Record<string, number>;
src/types/game.ts:    approval: number;
